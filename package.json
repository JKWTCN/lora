{
  "name": "lora",
  "private": true,
  "version": "0.1.2",
  "type": "module",
  "scripts": {
    "dev": "node sync-version.js && vite",
    "build": "node sync-version.js && vue-tsc --noEmit && vite build",
    "preview": "vite preview",
    "tauri": "tauri",
    "sync-version": "node sync-version.js"
  },
  "dependencies": {
    "vue": "^3.5.13",
    "@tauri-apps/api": "^2",
    "@tauri-apps/plugin-opener": "^2"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.2.1",
    "typescript": "~5.6.2",
    "vite": "^6.0.3",
    "vue-tsc": "^2.1.10",
    "@tauri-apps/cli": "^2"
  }
}